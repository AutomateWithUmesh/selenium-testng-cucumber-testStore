# selenium grid
selenium.grid.enabled=false
selenium.grid.urlFormat=http://%s:4444/wd/hub
selenium.grid.hubHost=localhost

# browser
browser=firefox

# application
test.store.url=https://teststore.automationtesting.co.uk/index.php

#credential path
credentialsFilePath=test-data/credentials.json
# using mvn package command, generate the package.
# Go to target/docker-resources folder and run the below command:

# $ java -Dbrowser=firefox -jar selenium-cucumber-docker-tests.jar -- Here browser value is passed explicitly, we have default value set in default.properties and pom.xml
